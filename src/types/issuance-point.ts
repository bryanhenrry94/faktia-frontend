export type IssuancePoint = {
  documentType: string;
  pointOfSale: string;
  organizationId: string;
  configId: string;
  createdAt: Date;
};

export type IssuancePointFormInput = {
  documentType: string;
  pointOfSale: string;
  organizationId: string;
  configId: string;
  createdAt: Date;
};
